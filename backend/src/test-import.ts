import { QuestNode } from "@pathweaver/shared-types";

const testNode: QuestNode = {
  id: "node-1",
  label: "Defeat Boss A",
  status: "available",
};

console.log("Shared type imported successfully:", testNode);
